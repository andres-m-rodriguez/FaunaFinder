# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fauna-finder
services:  
  app:
    language: dotnet
    project: ./src/FaunaFinder.AppHost/FaunaFinder.AppHost.csproj
    host: containerapp
